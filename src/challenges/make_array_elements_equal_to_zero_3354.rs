pub fn run() {
    let nums = Vec::from([1, 0, 2, 0, 3]);
    let result = count_valid_selections(nums);

    println!("result : {}", result);
}

fn count_valid_selections(nums: Vec<i32>) -> i32 {
    let mut stop = true;

    while stop {}
}
